spring:
  application:
    name: file-download-service
    
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jaydattdesai/Spring-cloud-config.git
        bootstrap: true